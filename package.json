{
  "name": "hueforge-web",
  "private": true,
  "version": "0.8.3-rc.3",
  "type": "module",
  "description": "Turn any image into a multi-layer 3D-printable filament painting (STL/3MF)",
  "main": "electron-main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "serve": "node server.mjs",
    "pack-deploy": "node scripts/pack-deploy.mjs",
    "electron:dev": "electron .",
    "electron:start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --win --x64",
    "dist:dir": "npm run build && electron-builder --win --x64 --dir"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "fflate": "^0.8.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/three": "^0.185.4",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "build": {
    "appId": "com.sovero.hueforge",
    "productName": "HueForge Desktop",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "dist/**/*",
      "electron-main.cjs",
      "electron-preload.cjs",
      "package.json"
    ],
    "win": {
      "executableName": "hueforge-desktop",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "signExecutable": false
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "HueForge Desktop",
      "artifactName": "hueforge-desktop-setup-${version}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Sovero",
        "repo": "hf",
        "private": true
      }
    ]
  }
}
