# cmd.exe читает .bat построчно и требует CRLF: с LF-переводами строк парсер
# теряет синхронизацию (блоки if/for и метки разбираются неверно), поэтому все
# пакетные файлы репозитория всегда выгружаются с CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
