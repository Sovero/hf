/**
 * Хрупкие участки собираются на этапе очистки, но не исправляются
 * автоматически — исправление не имеет смысла (арт-характером).
 */
export function noFixFor(_result: unknown): null {
  return null
}
